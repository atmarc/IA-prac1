Per executar la pràctica, simplement cal especificar els paràmetres desitjats
en el fitxer Main.java (usuaris, replicacions...), i descomentar la classe 
generadora d'estats successors adient per a cada algoritme i la funció que 
executa cada algoritme, que son runHillClimbing i runSimulatedAnealing.

